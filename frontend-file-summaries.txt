{
  "frontend/src/pages/QAPractice.tsx": "Q&A Practice page with setup, quiz, and review phases — generates questions from cert data, tracks pass/fail per answer, and shows detailed explanations for all questions in the review screen.",
  "frontend/src/pages/Flashcards.tsx": "Flashcard study page with setup, study, and review phases — generates and saves flashcard decks to the backend, supports card flip interaction, tracks correct/incorrect per card, and shows a full review with filtering.",
  "frontend/src/pages/PracticeTest.tsx": "Full mock exam page with setup, timed test, and results phases — generates 20+ questions, runs a countdown timer, saves test results to the backend, shows domain performance breakdown, and provides detailed per-question review with explanations.",
  "frontend/src/index.css": "Global OKLCH theme with comprehensive light and dark CSS variable sets (`.dark` selector overrides all tokens including background, foreground, card, primary, muted, accent, border, success, warning), `.cert-card` utility for uniform card sizing, and badge color helpers with dark mode variants.",
  "frontend/tailwind.config.js": "Tailwind configuration with `darkMode: ['class']` for class-based dark mode, OKLCH semantic color tokens mapped to CSS variables, Inter/Fira Code font families, custom border radius, and box shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — adds `useReadingProgress` (fetches all user PDF progress records via `getMyPdfProgressRecords`) and `useSaveReadingProgress` (mutation calling `saveOrUpdateProgress` with cache invalidation), alongside existing flashcard, question, and test result hooks.",
  "frontend/src/components/PDFViewer.tsx": "Full-screen PDF/HTML viewer with scroll-based reading progress tracking — attaches scroll listeners to iframe content document (HTML) or container div (PDF), debounces saves (600ms) to the backend via `useSaveReadingProgress`, restores scroll position on mount from saved percentage, and displays a live progress bar and percentage label in the header (desktop) and a mobile strip below the header.",
  "frontend/src/pages/StudyMaterials.tsx": "Study Materials page with 8 uniform `cert-card` certification cards using `border-2 border-border` outlines, standardized badge pills for all certs (A+ shows \"A+\" pill matching other certs, with short exam code \"220-1101\" instead of the long subtitle), reading progress bars, and a PDFViewer for opening guides.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page with 8 certification cards as the first section using `cert-card` uniform sizing and `border-2 border-border` outlines, standardized badge pills for all certs including A+, per-card test score progress via `useGetTestResults`, and quick-access Study/Cards/Test links using the correct `/study/$certificationId`, `/flashcards/$certificationId`, and `/test/$certificationId` routes.",
  "frontend/src/pages/CertificationSelector.tsx": "Certifications page with 8 standardized cards using `cert-card` uniform sizing and `border-2 border-border` outlines, consistent badge pills for all certs including A+, domain previews, exam details, and action buttons using the correct `/study/$certificationId`, `/flashcards/$certificationId`, and `/test/$certificationId` router paths.",
  "frontend/src/components/Layout.tsx": "Sticky header layout with logo, nav links, sun/moon icon theme toggle that applies/removes the `dark` class on `document.documentElement` with localStorage persistence (reads on init via useState lazy initializer, applies via useEffect), mobile nav, and footer with caffeine.ai attribution."
}