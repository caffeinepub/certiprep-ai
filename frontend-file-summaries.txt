{
  "frontend/src/pages/QAPractice.tsx": "Q&A Practice page with setup, quiz, and review phases — generates questions from cert data, tracks pass/fail per answer, and shows detailed explanations for all questions in the review screen.",
  "frontend/src/pages/Flashcards.tsx": "Flashcard study page with setup, study, and review phases — generates and saves flashcard decks to the backend, supports card flip interaction, tracks correct/incorrect per card, and shows a full review with filtering.",
  "frontend/src/pages/PracticeTest.tsx": "Full mock exam page with setup, timed test, and results phases — generates 20+ questions, runs a countdown timer, saves test results to the backend, shows domain performance breakdown, and provides detailed per-question review with explanations.",
  "frontend/src/index.css": "Global styles with OKLCH dark academia theme — fixes badge-green and badge-red to use plain CSS instead of @apply with custom color opacity modifiers, and adds --success/--success-foreground CSS variables so the success color works with Tailwind opacity utilities throughout the app.",
  "frontend/tailwind.config.js": "Tailwind configuration with dark academia color system — updates success color to use CSS variable format (oklch(var(--success) / alpha)) so opacity modifier utilities like bg-success/10 and text-success work correctly in component files.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — adds `useReadingProgress` (fetches all user PDF progress records via `getMyPdfProgressRecords`) and `useSaveReadingProgress` (mutation calling `saveOrUpdateProgress` with cache invalidation), alongside existing flashcard, question, and test result hooks.",
  "frontend/src/components/PDFViewer.tsx": "Full-screen PDF/HTML viewer with scroll-based reading progress tracking — attaches scroll listeners to iframe content document (HTML) or container div (PDF), debounces saves (600ms) to the backend via `useSaveReadingProgress`, restores scroll position on mount from saved percentage, and displays a live progress bar and percentage label in the header (desktop) and a mobile strip below the header.",
  "frontend/src/pages/StudyMaterials.tsx": "Study Materials page that renders 8 certification study guide cards at the top (with reading progress bars, file listings, open/download actions, and CTA buttons), followed by an info section below — fixes the TypeScript error by using `material.title` as the React list key instead of the non-existent `material.id` property.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page that now renders the 8 CompTIA certification cards (with Study/Flashcards/Test quick-access buttons and latest test scores) as the very first content section, followed by the hero welcome text, quick stats grid, and study modes banner below.",
  "frontend/src/pages/CertificationSelector.tsx": "Certification Selector page that now renders the 8 CompTIA certification cards (with difficulty badges, exam details, domain previews, and Study/Cards/Test action buttons) as the first content section, with the page title and description moved below the grid."
}